[package]
name = "imperator-save"
version = "0.4.2"
authors.workspace = true
edition = "2021"
exclude = ["/assets/*"]
license.workspace = true
readme = "README.md"
repository.workspace = true
description = "Ergonomically work with Imperator Rome saves (debug and ironman)"
keywords = ["imperator", "ironman"]
categories = ["parsing"]

[features]
serialize = []
zstd_c = ["jomini/zstd_c"]
zstd_rust = ["jomini/zstd_rust"]

[dependencies]
jomini = { workspace = true, features = ["envelope", "json"] }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
pdx-fixtures = { workspace = true }
flate2 = { workspace = true }
