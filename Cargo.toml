[workspace]
members = ["crates/*"]
resolver = "3"

[workspace.package]
authors = ["Nick Babcock <nbabcock19@hotmail.com>"]
license = "MIT"
repository = "https://github.com/rakaly/jomini"

[workspace.dependencies]
jomini = "0.36"
attohttpc = { version = "0.30.1", features = ["tls-vendored"] }
flate2 = { version = "1.1.5", default-features = false, features = ["zlib-rs"] }
rawzip = "0.5"
serde = { version = "1.0.195", features = ["derive"] }

# cargo-release settings. `cargo release -p jomini` is the only release that
# runs from this workspace.
[workspace.metadata.release]
tag-message = "Release {{version}}"
pre-release-commit-message = "Release {{version}}"
consolidate-commits = false

[patch.crates-io]
jomini = { path = "crates/jomini" }

[profile.release]
debug = true

[profile.bench]
debug = true
